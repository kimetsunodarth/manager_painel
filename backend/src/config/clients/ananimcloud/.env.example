# SSH Ananim Cloud (VM SUSE / CLOUDHDB)
# Copie para .env, preencha e rode: npm run encrypt-client-credentials -- ananimcloud
# O .env NÃO deve ser commitado; apenas credentials.enc (gerado) pode ficar no repositório.

SSH_HOST=CLOUDHDB
SSH_USER=root
SSH_PORT=22
SSH_PASSWORD=R@mo2278
